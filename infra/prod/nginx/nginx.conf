events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;
    error_log  /var/log/nginx/error.log warn;

    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout 65;
    client_max_body_size 25M;

    # =========================================================================
    # UPSTREAMS DO ECOSSISTEMA E-COMMERCE BOT
    # =========================================================================
    upstream core_api {
        server api:8080;
        keepalive 32;
    }

    upstream web_frontend {
        server web:80;
        keepalive 16;
    }

    # 🛡️ Zonas de Rate Limiting na Borda (Nginx Edge Protection)
    limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=5r/m;
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=120r/m;
    limit_req_status 429;

    # =========================================================================
    # 1. Bloco HTTP (Porta 80) - Desafios ACME / Certbot + Redirecionamento HTTPS
    # =========================================================================
    server {
        listen 80;
        server_name _;

        # Validação do Certbot para emissão/renovação de certificado SSL
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Redireciona todo o tráfego HTTP restante para HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # =========================================================================
    # 2. Bloco HTTPS (Porta 443) - Criptografia SSL/TLS de Produção
    # =========================================================================
    server {
        listen 443 ssl http2;
        server_name _;

        # Certificados SSL (Descomente após gerar a primeira vez via Certbot)
        ssl_certificate /etc/letsencrypt/live/seu-dominio.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/seu-dominio.com/privkey.pem;

        # Parâmetros de Segurança SSL Recomendados (TLS 1.2 / 1.3 apenas)
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Cabeçalhos de Segurança Globais (Hardening)
        add_header X-Frame-Options "DENY" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

        # 🚀 Rota Especial para SSE (Server-Sent Events) - Stream em tempo real sem buffering
        location /api/v1/demo/stream {
            proxy_pass http://core_api;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Desativa o buffering para entrega em tempo real
            proxy_buffering off;
            proxy_cache off;
            proxy_set_header X-Accel-Buffering "no";
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # 💳 Webhooks (Mercado Pago, Shopify, Nuvemshop, Resend) - Sem rate limit agressivo de IP
        location ~* ^/api/v1/(checkout/webhook|shopify/webhooks|nuvemshop/webhooks|emails/webhooks) {
            proxy_pass http://core_api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            client_max_body_size 10M;
        }

        # 🔒 Rota Autenticação com Rate Limiting estrito na borda
        location /api/v1/auth/ {
            limit_req zone=auth_limit burst=3 nodelay;
            proxy_pass http://core_api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # ⚡ Rotas de API Central (.NET 9 Core API)
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
            proxy_pass http://core_api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 🩺 Healthcheck da API Core
        location /health {
            proxy_pass http://core_api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 🌐 Rota Raiz - Frontend Web SPA (React 18 + Vite)
        location / {
            proxy_pass http://web_frontend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}